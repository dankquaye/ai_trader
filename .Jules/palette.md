## 2024-05-23 - Accessibility of Modal Close Buttons
**Learning:** Using `div` elements for interactive controls like modal close buttons creates accessibility barriers (no keyboard focus, no semantic meaning).
**Action:** Always use `<button>` for clickable elements and ensure icon-only buttons have `aria-label` attributes.
