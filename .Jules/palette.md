## 2024-05-18 - [Trade History Empty State]\n**Learning:** Tables displaying dynamic data, such as the Trade History table, should implement a helpful empty state with an icon and message when no data is present, rather than leaving the table body empty.\n**Action:** Implemented an empty state rendering logic in `app.js` using `colspan="7"` to span the table width.\n\n## 2026-07-03 - [Accessible Modal Dialog Pattern]\n**Learning:** The `#reasoning-modal` in `index.html` requires explicit `role="dialog"`, `aria-modal="true"`, and `aria-labelledby` attributes for screen reader support. Non-semantic interactive elements like `div.modal-close` must be replaced with semantic `<button type="button">` elements with `aria-label` attributes and visible focus states (`focus:ring-2`) for keyboard accessibility.\n**Action:** Added semantic roles to the modal container and replaced the icon `div` and footer button with accessible `<button>` tags featuring ARIA labels and Tailwind focus utility classes in `index.html`.
