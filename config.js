/**
 * Global Configuration for Deriv API
 * These values are externalized to allow for easier configuration and avoid hardcoding in core logic.
 */
window.DerivConfig = {
    demo: {
        appId: 71238
    },
    live: {
        appId: 71236
    }
};
